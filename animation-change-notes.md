# Work Address Resolver animation revision

The previous perpetual gantry scan was visually subtle inside a dense machine frame. The replacement uses a one-shot causal sequence: a selected product signal enters, five governed work coordinates resolve, the evidence state locks, and a learning signal returns to the product layer.

The final resolved state remains visible. Scenario selection replays the sequence once. Reduced-motion users receive the resolved state immediately.
