const scenarios={knowledge:{surface:'Knowledge Capture & Transfer',platform:'Versioned work ontology',decision:'Make expertise retrievable without separating it from the work, role, equipment, and approved procedure it belongs to.',tag:'WORK://EXPERT-CAPTURE/ROLE-04/V2',state:'Knowledge addressed'},output:{surface:'Operational Output Verification',platform:'Output evidence contract',decision:'Define the observable result, tolerance, confidence posture, exception class, and human review before automating the judgment.',tag:'WORK://OUTPUT-VERIFY/LOT-118/V1',state:'Output evidence set'},live:{surface:'Live SOP Verification',platform:'Sequence + latency contract',decision:'Match model confidence and response time to the consequence of a missed, late, or incorrect intervention—and preserve human authority.',tag:'WORK://LIVE-SOP/STEP-07/V3',state:'Drift threshold set'},motion:{surface:'Time & Motion AI',platform:'Cycle and activity event model',decision:'Keep activity classification tied to the same work identity so timing variation can return as a useful improvement hypothesis.',tag:'WORK://CYCLE-ANALYSIS/CELL-12/V5',state:'Flow address resolved'}};
function setScenario(key,focus=false){const d=scenarios[key]||scenarios.knowledge;document.querySelectorAll('.scenario-tab').forEach(b=>b.setAttribute('aria-selected',String(b.dataset.scenario===key)));const ids={surface:'surface-value',platform:'platform-value',decision:'decision-value',tag:'address-tag',state:'stage-state'};Object.entries(ids).forEach(([k,id])=>{const el=document.getElementById(id);if(el)el.textContent=d[k]});const stage=document.getElementById('work-coordinate');if(stage){stage.classList.remove('is-scanning');void stage.offsetWidth;stage.classList.add('is-scanning')}if(focus){document.getElementById('surface-value')?.focus()}}
document.addEventListener('DOMContentLoaded',()=>{const toggle=document.querySelector('.mobile-toggle'),links=document.getElementById('site-links');toggle?.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));links?.classList.toggle('open',!open)});document.querySelectorAll('.scenario-tab').forEach((button,i,buttons)=>{button.addEventListener('click',()=>setScenario(button.dataset.scenario));button.addEventListener('keydown',e=>{if(!['ArrowRight','ArrowLeft','Home','End'].includes(e.key))return;e.preventDefault();let n=i;if(e.key==='ArrowRight')n=(i+1)%buttons.length;if(e.key==='ArrowLeft')n=(i-1+buttons.length)%buttons.length;if(e.key==='Home')n=0;if(e.key==='End')n=buttons.length-1;buttons[n].focus();setScenario(buttons[n].dataset.scenario)})});document.getElementById('reset-scenario')?.addEventListener('click',()=>setScenario('knowledge'));setScenario('knowledge')});